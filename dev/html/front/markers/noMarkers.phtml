<ips:template parameters="$group" />

<div class='ipsType_center ipsPad'>
	<p class='ipsType_large'>{lang="membermap_no_markers_in_cat"}</p>

	{{if $group->can('add')}}
		<a href='{url="app=membermap&module=markers&controller=submit&_new=1&group={$group->id}" seoTemplate="membermap_submit"}' class='ipsButton ipsButton_primary ipsButton_medium'>{lang="membermap_submit_first_marker"}</a>
	{{endif}}
</div>